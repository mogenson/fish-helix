function fish_default_mode_prompt --description "Display vi/helix prompt mode"
    # This function is intentionally empty to disable mode indicators in prompt
    # Mode will be indicated by cursor shape instead
end
